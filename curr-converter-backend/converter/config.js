const config = {
    LoadSymbolsURL: "https://api.exchangeratesapi.io/latest",
    ExternalAPIURL: "https://api.exchangeratesapi.io/latest?symbols={base},{dest}&base={base}",
    
}

export default config;